# URL del backend (API). Se usa para construir endpoints (ej. módulo `/terms`).
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# URL del sitio (links absolutos, redirects, etc.)
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# (Opcional - solo desarrollo) Mock token para previsualizar `/terms` con flujo “con token”.
# Uso: definir un token real/temporal para probar UI sin query param.
NEXT_PUBLIC_TERMS_ACCEPT_MOCK_TOKEN=